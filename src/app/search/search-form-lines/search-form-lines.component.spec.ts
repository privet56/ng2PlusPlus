/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SearchFormLinesComponent } from './search-form-lines.component';

describe('Component: SearchFormLines', () => {
  it('should create an instance', () => {
    let component = new SearchFormLinesComponent(null, null);
    expect(component).toBeTruthy();
  });
});
