/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SearchFormLineComponent } from './search-form-line.component';

describe('Component: SearchFormLine', () => {
  it('should create an instance', () => {
    let component = new SearchFormLineComponent(null);
    expect(component).toBeTruthy();
  });
});
