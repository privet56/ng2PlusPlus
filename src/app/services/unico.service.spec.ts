/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UnicoService } from './unico.service';

describe('Service: Unico', () => {
  beforeEach(() =>
  {
    /*
    TestBed.configureTestingModule({
      providers: [UnicoService]
    });
    */
  });
  
  it(' unicoService should check NOTHING ', () /*inject([UnicoService], (service: UnicoService)*/ => {
    //expect(service).toBeTruthy();
  });

});
